<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item
        android:id="@+id/ring_indicator_shape"
        android:drawable="@drawable/ring_indicator_thick_blue"/>

    <item
        android:id="@+id/ring_indicator_clipping_mask">
        <clip
            xmlns:android="http://schemas.android.com/apk/res/android"
            android:clipOrientation="vertical"
            android:drawable="@drawable/ring_indicator_thin_white"
            android:gravity="top"/>
    </item>

</layer-list>